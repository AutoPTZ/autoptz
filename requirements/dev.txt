# AutoPTZ — development & test dependencies
# Install after base:
#   python tools/install.py --dev --editable

# ── testing ───────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-timeout==2.3.1
onnx==1.17.0            # create minimal ONNX models in tests

# ── linting / type checking ───────────────────────────────────────────────────
ruff==0.9.10
mypy==1.15.0
