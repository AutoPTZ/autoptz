# AutoPTZ v2 — development & test dependencies
# Install after base:
#   pip install -r requirements/base.txt -r requirements/dev.txt

# ── testing ───────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-timeout==2.3.1
onnx==1.17.0            # create minimal ONNX models in tests

# ── linting / type checking ───────────────────────────────────────────────────
ruff==0.9.10
mypy==1.15.0
