# AutoPTZ — NVIDIA GPU extras (Windows x64 / Linux x64)
#
# Install after base:
#   pip uninstall -y onnxruntime onnxruntime-gpu onnxruntime-directml onnxruntime-openvino
#   pip install -r requirements/gpu-nvidia.txt
#
# Provides: TensorRT EP (best) → CUDA EP → CPU fallback.
# TensorRT EP requires TensorRT 10.x installed separately from NVIDIA.
# CUDA EP requires CUDA 12.x + cuDNN 9.x.

onnxruntime-gpu==1.24.1
