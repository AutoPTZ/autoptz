"""Per-camera pipeline process: ingest → detect → track → reid → identify → pose → PTZ.

Phase 6 implementation target.
"""
from __future__ import annotations

# TODO(phase-6): implement CameraWorker process entry point
